# 🤖 MSDS智能导入工具 - 使用说明

> **完全自动化！** 只需指定文件夹，自动识别化学品信息并导入数据库！

---

## ✨ 新功能亮点

### 🎯 自动识别

从 HTML 文件自动提取：
- ✅ 化学品中文名称
- ✅ CAS号
- ✅ 英文名称
- ✅ 分子式
- ✅ EC编号

### 🚀 智能创建

- ✅ 如果化学品不存在，自动创建
- ✅ 自动创建MSDS文档
- ✅ 自动导入16个章节
- ✅ 支持更新已有数据

---

## 📊 新旧版本对比

| 功能 | 旧版 | 新版 |
|------|------|------|
| 输入CAS号 | ❌ 必须手动输入 | ✅ 自动识别 |
| 创建化学品 | ❌ 手动创建 | ✅ 自动创建 |
| 操作步骤 | 3步 | 1步 |

---

## 🚀 使用方式

### 方式1：图形界面（最简单）⭐

```bash
# 双击运行
导入MSDS到数据库.bat

# 输入文件夹名称
msds_甲苯

# CAS号：直接回车（自动识别）
[回车]

# 密码：输入或回车使用默认值
123456

# 完成！
```

---

### 方式2：命令行（推荐）

```bash
# 自动识别模式（推荐）
python import_msds_to_db.py --folder msds_甲苯

# 指定密码
python import_msds_to_db.py -f msds_甲苯 -p 你的密码

# 手动指定CAS号（可选）
python import_msds_to_db.py -f msds_甲苯 -c 108-88-3
```

---

## 📋 完整示例

### 示例1：导入甲苯（自动识别）

```bash
python import_msds_to_db.py --folder msds_甲苯
```

**输出**：
```
======================================================================
MSDS数据导入工具 - 智能版
======================================================================
文件夹: msds_甲苯
======================================================================

📍 步骤1：自动识别化学品信息...
✓ 化学品名称: 甲苯
✓ CAS号: 108-88-3
✓ 英文名: toluene
✓ 分子式: C7H8

✓ 数据库连接成功
⚠ 数据库中未找到该化学品，自动创建...
✓ 已创建化学品记录，ID: 3
✓ MSDS文档ID: 3

  插入章节 01: 化学品及企业标识
  插入章节 02: 危险性概述
  ...
  插入章节 16: 其他信息

======================================================================
导入完成! 成功导入 16/16 个章节
======================================================================
```

---

### 示例2：批量导入

创建 `batch_import.bat`：

```batch
@echo off
echo 开始批量导入...

python import_msds_to_db.py -f msds_甲苯
python import_msds_to_db.py -f msds_乙醇无水
python import_msds_to_db.py -f msds_丙酮

echo 全部完成！
pause
```

---

## 🔍 工作原理

```
┌─────────────────────────────────┐
│  指定MSDS文件夹                  │
│  （只需要文件夹名）               │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│  自动读取01_化学品及企业标识.html│
│  • 提取中文名：甲苯              │
│  • 提取CAS号：108-88-3          │
│  • 提取分子式：C7H8             │
│  • 提取EC编号：203-625-9        │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│  检查数据库中是否存在该化学品     │
│  • 存在 → 使用现有记录           │
│  • 不存在 → 自动创建新记录       │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│  创建MSDS文档                    │
│  • 一对一关联化学品              │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│  导入16个章节                    │
│  • 解析每个HTML文件              │
│  • 提取纯文本和结构化数据         │
│  • 插入MSDS章节表                │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│  完成！可以在Web应用中查询       │
└─────────────────────────────────┘
```

---

## 🆚 完整对比

### 旧版流程（3步）

```
步骤1：查找CAS号
  - 打开网站或文档
  - 手动查找CAS号：108-88-3

步骤2：在数据库中创建化学品
  INSERT INTO 化学品 (CAS号, 中文名, ...) 
  VALUES ('108-88-3', '甲苯', ...);

步骤3：运行导入脚本
  python import_msds_to_db.py -f msds_甲苯 -c 108-88-3
```

### 新版流程（1步）⭐

```
步骤1：运行导入脚本
  python import_msds_to_db.py -f msds_甲苯
  
  （其他全自动完成！）
```

---

## 💡 特殊场景

### 场景1：化学品已存在

```bash
# 第一次导入
python import_msds_to_db.py -f msds_甲苯
# 输出：✓ 已创建化学品记录，ID: 3

# 第二次导入（更新MSDS）
python import_msds_to_db.py -f msds_甲苯
# 输出：✓ 找到化学品ID: 3
```

---

### 场景2：手动指定CAS号

```bash
# 如果HTML文件损坏或格式不对
python import_msds_to_db.py -f msds_甲苯 -c 108-88-3

# 跳过自动识别，直接使用指定的CAS号
```

---

### 场景3：批量导入

```python
# batch_import.py
from import_msds_to_db import import_msds_folder
import os

# 自动扫描所有msds_开头的文件夹
for folder in os.listdir('.'):
    if folder.startswith('msds_') and os.path.isdir(folder):
        print(f"\n导入 {folder}...")
        import_msds_folder(folder)

print("\n批量导入完成！")
```

---

## ❓ 常见问题

### Q1: 提示"无法提取化学品信息"

**A**: 检查是否有 `01_化学品及企业标识.html` 文件

```bash
# 查看文件夹内容
dir msds_甲苯

# 应该看到：
# 01_化学品及企业标识.html
# 02_危险性概述.html
# ...
```

---

### Q2: 如何验证导入成功？

**A**: 使用SQL查询

```sql
-- 查看化学品
SELECT * FROM 化学品 WHERE CAS号 = '108-88-3';

-- 查看MSDS章节
SELECT 章节序号, 章节标题 
FROM MSDS章节 
WHERE 文档编号 = (
    SELECT 编号 FROM MSDS文档 
    WHERE 化学品编号 = (
        SELECT 编号 FROM 化学品 WHERE CAS号 = '108-88-3'
    )
);
```

---

### Q3: 导入后如何查询？

**A**: 使用Web应用

```bash
# 启动Web应用
启动Web应用.bat

# 浏览器访问
http://localhost:5000

# 搜索"甲苯"，查看完整MSDS信息
```

---

### Q4: 如何重新导入？

**A**: 再次运行即可，会自动更新

```bash
# 第一次导入
python import_msds_to_db.py -f msds_甲苯

# 修改HTML文件后重新导入
python import_msds_to_db.py -f msds_甲苯
# 会更新现有数据
```

---

### Q5: 如何删除已导入的数据？

**A**: 删除会级联删除MSDS

```sql
-- 删除化学品（会自动删除MSDS文档和所有章节）
DELETE FROM 化学品 WHERE CAS号 = '108-88-3';
```

---

## 🎯 最佳实践

### 完整工作流

```
1. 爬取数据
   智能爬虫.bat → msds_甲苯/

2. 导入数据库（自动识别）
   导入MSDS到数据库.bat → 输入文件夹名 → 回车

3. Web应用查询
   启动Web应用.bat → 搜索化学品

4. AI大模型调用
   调用API获取MSDS数据
```

---

### 数据维护

```sql
-- 查看已导入的化学品
SELECT 
    p.中文名,
    p.CAS号,
    COUNT(c.编号) AS 章节数
FROM 化学品 p
LEFT JOIN MSDS文档 m ON p.编号 = m.化学品编号
LEFT JOIN MSDS章节 c ON m.编号 = c.文档编号
GROUP BY p.编号, p.中文名, p.CAS号
HAVING COUNT(c.编号) > 0;
```

---

## 📚 相关工具链

```
智能爬虫 → 智能导入 → Web应用
   ↓          ↓          ↓
自动识别   自动入库   AI调用
```

| 工具 | 功能 | 文件 |
|------|------|------|
| **智能爬虫** | 自动爬取MSDS | `智能爬虫.bat` |
| **智能导入** | 自动识别入库 | `导入MSDS到数据库.bat` |
| **Web应用** | 查询展示 | `启动Web应用.bat` |

---

## 🎊 总结

### 核心优势

✅ **完全自动化** - 只需指定文件夹  
✅ **智能识别** - 自动提取化学品信息  
✅ **智能创建** - 不存在则自动创建  
✅ **支持更新** - 可重复导入更新数据  
✅ **批量处理** - 支持批量导入脚本

---

### 对比其他方案

| 方案 | 操作步骤 | 出错概率 |
|------|---------|---------|
| **智能导入** ⭐ | 1步 | 低 |
| 手动SQL | 5-10步 | 高 |
| Excel导入 | 3-5步 | 中 |

---

## 🚀 立即开始

```bash
# 最简单的方式
双击：导入MSDS到数据库.bat
输入：msds_甲苯
回车：（自动识别）
回车：（使用默认密码）
完成！
```

**就这么简单！** 🎉

---

**版本**: v2.0 (智能版)  
**更新日期**: 2025-10-10  
**Python要求**: 3.6+  
**依赖**: pymysql, beautifulsoup4


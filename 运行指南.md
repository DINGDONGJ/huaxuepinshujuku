# 项目运行指南

这份文档按仓库当前实际状态编写，适用于在本机启动这个项目。默认以 Windows + PowerShell 为主，Linux 也可按同样思路执行。

## 1. 先说结论

如果你只想把 Web 查库功能跑起来，最小闭环是：

1. 安装 Python 和 MySQL
2. 导入 `database_backup_clean_utf8.sql`
3. 安装 `new_db/requirements.txt` 里的基础依赖
4. 运行 `new_db/app.py`
5. 打开 `http://localhost:5001`

如果你还想启用语义搜索、爬虫或 AI 分析，再额外装对应依赖。

## 2. 环境要求

推荐环境：

- Python 3.10 或 3.11
- MySQL 8.0+
- Windows PowerShell 或 CMD

可选能力对应的额外要求：

- 语义搜索：`sentence-transformers`、`scikit-learn`
- 爬虫：`beautifulsoup4`、`playwright`
- AI 分析：
  - 本地 Ollama，或
  - OpenAI 兼容接口，或
  - 硅基流动接口

## 3. 代码里当前的数据库配置

当前代码里数据库配置是硬编码的，主要在两个文件：

- `new_db/app.py`
- `new_db/build_semantic_index.py`

默认值是：

```python
host = localhost
user = root
password = 1234
database = 危化品简化数据库
```

你有两种做法：

### 方案 A：沿用当前默认值

让本机 MySQL 的 `root` 用户密码就是 `1234`。

### 方案 B：修改代码中的数据库配置

把以下两个文件里的 `DB_CONFIG` 改成你自己的 MySQL 连接信息：

- `new_db/app.py`
- `new_db/build_semantic_index.py`

如果你只是想先跑通项目，我建议先统一成一个能用的本地连接配置，再继续下面步骤。

## 4. 创建虚拟环境

在 PowerShell 里执行：

```powershell
cd d:\huaxuepinshujuku\new_db
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

如果 PowerShell 禁止执行脚本，可以先运行：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
```

然后再激活虚拟环境。

## 5. 安装基础依赖

先安装 Web 运行所需的最小依赖：

```powershell
pip install -r requirements.txt
```

这一步安装的是：

- Flask
- PyMySQL
- requests

安装完成后，单纯的 Web 查库功能就具备基础运行条件了。

## 6. 导入数据库

当前工作区中可直接导入的备份文件是根目录的：

```text
d:\huaxuepinshujuku\database_backup_clean_utf8.sql
```

不要优先相信旧脚本里提到的 `database_backup.sql`，当前工作区没有这个文件。

### 推荐导入方式

先进入 MySQL：

```powershell
mysql -u root -p
```

进入 MySQL 后执行：

```sql
CREATE DATABASE IF NOT EXISTS `危化品简化数据库`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_0900_ai_ci;

USE `危化品简化数据库`;

SOURCE D:/huaxuepinshujuku/database_backup_clean_utf8.sql;
```

导入完成后可执行校验：

```sql
SELECT COUNT(*) AS total_chemicals FROM 化学品;
SELECT COUNT(*) AS total_msds_docs FROM `MSDS文档`;
SELECT COUNT(*) AS total_msds_chapters FROM `MSDS章节`;
```

当前备份对应的预期数量是：

- `化学品`：2609
- `MSDS文档`：2609
- `MSDS章节`：41736

## 7. 启动 Web 服务

确认你还在虚拟环境中，然后执行：

```powershell
cd d:\huaxuepinshujuku\new_db
python app.py
```

启动后访问：

```text
http://localhost:5001
```

如果一切正常，你会看到“易链危化品智能查询系统”的页面。

## 8. 可选：启用语义搜索

语义搜索不是必需功能。当前仓库里虽然有 `new_db/semantic_cache`，但缓存是旧的，不对应全量数据，建议重新构建。

### 安装依赖

```powershell
pip install sentence-transformers scikit-learn
```

### 构建索引

```powershell
cd d:\huaxuepinshujuku\new_db
python build_semantic_index.py
```

构建成功后，`/api/semantic-search` 和 `/api/semantic-status` 才算真正可用。

## 9. 可选：启用爬虫

如果你需要从外部页面抓取 MSDS 并导入库里，再安装下面的依赖。

### 安装依赖

```powershell
pip install beautifulsoup4 playwright
python -m playwright install chromium
```

### 使用方式

只抓取 JSON：

```powershell
cd d:\huaxuepinshujuku\new_db
python scrape_to_json.py "目标MSDS页面URL"
```

抓取并直接导入数据库：

```powershell
python scrape_to_json.py "目标MSDS页面URL" --import --password 你的MySQL密码
```

如果你的数据库密码已经在代码里改好了，命令中的密码也要和实际数据库一致。

## 10. 可选：启用 AI 相容性分析

AI 分析接口是：

```text
/api/compatibility-ai
```

当前后端逻辑在 `new_db/ai_analyzer.py`，默认优先使用：

```text
AI_BACKEND=ollama
```

也就是说，默认需要本地启动 Ollama。

### 用 Ollama

先启动服务：

```powershell
ollama serve
```

再拉取默认模型：

```powershell
ollama pull qwen2.5:7b
```

### 用其他 OpenAI 兼容接口

需要设置环境变量，例如：

```powershell
$env:AI_BACKEND="openai"
$env:OPENAI_API_KEY="你的Key"
$env:OPENAI_API_BASE="你的兼容接口地址"
$env:OPENAI_MODEL="你的模型名"
```

然后再启动 `python app.py`。

## 11. 不建议优先使用的旧脚本

仓库里有一些历史脚本，但不建议把它们当作第一启动入口：

- `new_db/start_web.bat`
  - 只是简单执行 `python app.py`
  - 不能替代环境准备和数据库导入

建议优先按本指南手动执行一遍，先确保你理解系统依赖和数据入口。

## 12. 常见问题

### 1. 启动时报 `ModuleNotFoundError: flask`

说明基础依赖没装：

```powershell
pip install -r d:\huaxuepinshujuku\new_db\requirements.txt
```

### 2. 语义搜索接口返回“未启用”

说明少了语义搜索依赖，或者还没构建索引：

```powershell
pip install sentence-transformers scikit-learn
cd d:\huaxuepinshujuku\new_db
python build_semantic_index.py
```

### 3. 爬虫运行时报 `ModuleNotFoundError: playwright`

说明爬虫依赖未安装：

```powershell
pip install beautifulsoup4 playwright
python -m playwright install chromium
```

### 4. 导入数据库后启动报表不存在

先确认导入的是根目录的：

```text
database_backup_clean_utf8.sql
```

再确认当前数据库名就是：

```text
危化品简化数据库
```

### 5. Linux 部署时报 `msds文档` 或 `msds章节` 不存在

当前代码里存在大小写混用：

- 建表脚本使用 `MSDS文档`、`MSDS章节`
- `app.py` 里很多查询写的是 `msds文档`、`msds章节`

在 Windows 上通常不明显，但在 Linux + MySQL 某些配置下会直接失败。  
如果你准备部署 Linux，建议先统一表名大小写后再上线。

## 13. 建议的首次验证顺序

启动后建议先做这几步验证：

1. 打开首页，确认页面能加载。
2. 搜索一个常见化学品，例如 `甲苯`、`乙醇` 或 `苯`。
3. 确认能看到基础信息和 MSDS 章节。
4. 打开法规推荐，确认法规列表能返回。
5. 如果启用了语义搜索，再测试自然语言查询。

如果你后面要继续接手开发，建议下一步优先处理：

1. 把数据库配置改成环境变量。
2. 清理明文密码和明文 API Key。
3. 统一 `MSDS文档/MSDS章节` 的表名大小写。
4. 重建语义索引。

# 多化学品共存禁忌评估功能实现计划

## 📋 需求概述

**甲方需求：**
- 支持同时查询多个化学品（如"甲醛和丙醇"）
- 判断多个化学品的共存禁忌
- 评估多个化学品混合后的危害程度

---

## 🔍 当前系统分析

### 1. 现有功能

✅ **单个化学品查询**
- API: `POST /api/search`
- 支持：中文名、英文名、CAS号、别名
- 限制：`LIMIT 1`，只返回第一个匹配结果

✅ **MSDS数据结构**
- 第10章：稳定性和反应性
  - 包含字段：`禁配物`、`不相容的物质`
  - 存储格式：纯文本（LONGTEXT）
  - 示例内容："不相容的物质：强氧化剂、强酸、强碱"

✅ **数据库结构**
- `MSDS章节` 表存储章节内容
- 全文索引支持文本搜索

### 2. 数据基础评估

**✅ 可用的数据源：**
1. MSDS第10章（稳定性和反应性）- 包含不相容物质信息
2. MSDS第2章（危险性概述）- 包含GHS分类和危险性类别
3. MSDS第9章（理化特性）- 包含闪点、爆炸极限等
4. MSDS第11章（毒理学信息）- 包含毒性数据

**⚠️ 数据限制：**
- 不相容物质信息以**自由文本**形式存储，需要文本解析
- 没有结构化的化学品反应数据库
- 需要匹配化学品名称（可能有多种表述方式）

---

## 🎯 实现方案

### 方案A：基于MSDS文本解析（推荐，快速实现）

**核心思路：**
1. 解析每个化学品MSDS第10章的"不相容物质"文本
2. 提取其中的化学品名称
3. 交叉比对：检查化学品A的不相容列表是否包含化学品B
4. 综合多个化学品的危险性分类，评估混合后的危害

**优点：**
- 实现快速，利用现有数据
- 无需额外数据源
- 可立即上线

**缺点：**
- 依赖文本解析准确性
- 可能需要人工维护化学品名称映射表

### 方案B：构建化学品反应知识库（长期方案）

**核心思路：**
1. 新建数据库表存储化学品反应规则
2. 基于MSDS数据构建知识库
3. 支持结构化查询和推理

**优点：**
- 准确度高
- 可扩展性强
- 支持复杂规则

**缺点：**
- 开发周期长
- 需要大量数据整理工作

---

## 📐 推荐实现方案：方案A（分阶段实施）

### 阶段1：多化学品查询功能（1-2周）

#### 1.1 前端修改

**搜索框增强：**
- 支持多关键词输入（分隔符：`、`、`,`、`和`、`与`）
- 示例：`甲醛、丙醇` 或 `甲醛和丙醇`
- UI：显示多个化学品卡片，每个卡片显示基本信息

**实现位置：**
- `index.html` - 搜索框输入处理（第2474行）
- `index.html` - 搜索结果展示（第2690行）

**修改点：**
```javascript
// 1. 解析多关键词
function parseMultipleKeywords(input) {
    // 支持：甲醛、丙醇 | 甲醛和丙醇 | 甲醛 丙醇
    return input.split(/[、,和与\s]+/).map(k => k.trim()).filter(k => k);
}

// 2. 调用新API查询多个化学品
async function searchMultiple(keywords) {
    const response = await fetch('/api/search-multiple', {
        method: 'POST',
        body: JSON.stringify({ keywords: keywords })
    });
}
```

#### 1.2 后端API新增

**新接口：** `POST /api/search-multiple`

**功能：**
- 接收多个关键词数组
- 返回每个化学品的完整信息
- 处理未找到的化学品

**实现位置：**
- `app.py` - 新增路由（第350行后）

**SQL查询：**
```python
# 批量查询化学品
chemicals = []
for keyword in keywords:
    cursor.execute("""
        SELECT 编号 FROM 化学品 
        WHERE 中文名 LIKE %s OR 英文名 LIKE %s OR CAS号 = %s
        LIMIT 1
    """, (f'%{keyword}%', f'%{keyword}%', keyword))
    # ... 获取完整信息
```

**返回格式：**
```json
{
    "chemicals": [
        {
            "basic_info": {...},
            "msds_chapters": [...],
            "found": true
        },
        {
            "basic_info": {...},
            "msds_chapters": [...],
            "found": true
        }
    ],
    "not_found": ["丙醇"]  // 未找到的化学品
}
```

---

### 阶段2：共存禁忌分析功能（2-3周）

#### 2.1 文本解析模块

**功能：** 从MSDS第10章提取不相容物质列表

**实现位置：**
- `app.py` - 新增函数 `extract_incompatible_substances(chapter_content)`

**解析策略：**
```python
def extract_incompatible_substances(content):
    """
    从第10章内容中提取不相容物质
    示例文本：
    "不相容的物质：强氧化剂、强酸、硝酸、过氧化氢"
    "禁配物：金属、水"
    """
    incompatible = []
    
    # 匹配模式
    patterns = [
        r'不相容[的]?物质[：:]\s*(.+?)(?:\n|$)',
        r'禁配物[：:]\s*(.+?)(?:\n|$)',
        r'应避免[与]?接触[的]?物质[：:]\s*(.+?)(?:\n|$)'
    ]
    
    for pattern in patterns:
        matches = re.findall(pattern, content, re.IGNORECASE)
        for match in matches:
            # 分割：支持多种分隔符
            substances = re.split(r'[、,，;；/和与]+', match)
            incompatible.extend([s.strip() for s in substances if s.strip()])
    
    return list(set(incompatible))  # 去重
```

#### 2.2 化学品名称匹配

**功能：** 将提取的不相容物质名称匹配到数据库中的化学品

**挑战：**
- 文本中可能是通用类别（如"强氧化剂"）
- 可能是化学品名称的不同表述
- 可能是CAS号

**解决方案：**
```python
def match_substance_to_chemical(substance_name, chemicals_list):
    """
    将物质名称匹配到化学品
    匹配优先级：
    1. 精确匹配（中文名、英文名、CAS号）
    2. 模糊匹配（包含关系）
    3. 类别匹配（强氧化剂、强酸等）
    """
    matched = []
    
    for chem in chemicals_list:
        # 精确匹配
        if (substance_name == chem['中文名'] or 
            substance_name == chem['英文名'] or
            substance_name == chem['CAS号']):
            matched.append(chem)
            continue
        
        # 模糊匹配
        if (substance_name in chem['中文名'] or 
            substance_name in chem['英文名']):
            matched.append(chem)
    
    return matched
```

#### 2.3 共存禁忌分析API

**新接口：** `POST /api/compatibility-check`

**功能：**
- 接收多个化学品ID
- 提取每个化学品的不相容物质
- 交叉比对，找出禁忌组合
- 返回分析结果

**实现位置：**
- `app.py` - 新增路由（第650行后）

**核心逻辑：**
```python
@app.route('/api/compatibility-check', methods=['POST'])
def check_compatibility():
    data = request.get_json()
    chemical_ids = data.get('chemical_ids', [])
    
    # 1. 获取每个化学品的第10章内容
    incompatibilities = {}
    for chem_id in chemical_ids:
        chapter10 = get_chapter_10(chem_id)
        incompatible = extract_incompatible_substances(chapter10['内容'])
        incompatibilities[chem_id] = incompatible
    
    # 2. 交叉比对
    conflicts = []
    for i, chem_id_1 in enumerate(chemical_ids):
        for chem_id_2 in chemical_ids[i+1:]:
            chem1_info = get_chemical_info(chem_id_1)
            chem2_info = get_chemical_info(chem_id_2)
            
            # 检查chem1的不相容列表是否包含chem2
            chem1_incompatible = incompatibilities[chem_id_1]
            if is_incompatible(chem2_info, chem1_incompatible):
                conflicts.append({
                    'chemical_1': chem1_info['中文名'],
                    'chemical_2': chem2_info['中文名'],
                    'reason': f"{chem1_info['中文名']}与{chem2_info['中文名']}不相容",
                    'severity': 'high'  # 待实现危害评估
                })
    
    return jsonify({
        'chemicals': [...],
        'conflicts': conflicts,
        'safe': len(conflicts) == 0
    })
```

---

### 阶段3：危害程度评估功能（2-3周）

#### 3.1 危害等级计算

**评估维度：**

1. **不相容性等级**
   - 高：明确标记为"禁配物"或"剧烈反应"
   - 中：标记为"不相容"
   - 低：标记为"避免接触"

2. **GHS危险性叠加**
   - 易燃 + 易燃 = 极高风险
   - 氧化性 + 易燃 = 极高风险
   - 有毒 + 有毒 = 高风险

3. **反应性风险**
   - 爆炸性反应
   - 放热反应
   - 产生有毒气体

**实现函数：**
```python
def assess_hazard_level(chemicals, conflicts):
    """
    评估多个化学品混合后的危害等级
    返回：'low' | 'medium' | 'high' | 'extreme'
    """
    risk_score = 0
    
    # 1. 不相容性评分
    for conflict in conflicts:
        if conflict['severity'] == 'high':
            risk_score += 10
        elif conflict['severity'] == 'medium':
            risk_score += 5
    
    # 2. GHS分类叠加评分
    ghs_categories = []
    for chem in chemicals:
        chapter2 = get_chapter_2(chem['编号'])
        categories = extract_ghs_categories(chapter2['内容'])
        ghs_categories.extend(categories)
    
    # 检查危险组合
    if '易燃液体' in ghs_categories and '氧化性' in ghs_categories:
        risk_score += 15  # 易燃+氧化 = 极高风险
    
    if '强酸' in ghs_categories and '强碱' in ghs_categories:
        risk_score += 10  # 酸碱中和放热
    
    # 3. 毒性叠加
    toxic_count = sum(1 for cat in ghs_categories if '毒性' in cat)
    if toxic_count >= 2:
        risk_score += 8
    
    # 评分转换为等级
    if risk_score >= 20:
        return 'extreme'
    elif risk_score >= 10:
        return 'high'
    elif risk_score >= 5:
        return 'medium'
    else:
        return 'low'
```

#### 3.2 风险评估报告生成

**报告内容：**
1. **共存安全性评估**
   - ✅ 安全 / ⚠️ 警告 / ❌ 危险
   - 总体风险等级

2. **禁忌组合列表**
   - 每对不相容的化学品
   - 不相容原因
   - 风险评估

3. **建议措施**
   - 存储建议
   - 操作建议
   - 应急措施

---

## 🗄️ 数据库扩展（可选）

### 新增表：化学品反应知识库

```sql
CREATE TABLE 化学品反应规则 (
    编号 BIGINT PRIMARY KEY AUTO_INCREMENT,
    化学品A编号 BIGINT NOT NULL,
    化学品B编号 BIGINT NOT NULL,
    反应类型 VARCHAR(64),  -- '不相容', '剧烈反应', '放热反应'
    危险等级 VARCHAR(16),  -- 'low', 'medium', 'high', 'extreme'
    反应描述 TEXT,
    建议措施 TEXT,
    UNIQUE KEY 唯一键_反应对 (化学品A编号, 化学品B编号)
);
```

**数据来源：**
- 从MSDS第10章自动提取
- 手动维护补充

---

## 📊 前端UI设计

### 多化学品查询界面

```
┌─────────────────────────────────────────┐
│ 搜索框：[甲醛、丙醇                ] [搜索] │
└─────────────────────────────────────────┘

查询结果：

┌─────────────────┐  ┌─────────────────┐
│ 化学品1：甲醛    │  │ 化学品2：丙醇   │
│ CAS: 50-00-0    │  │ CAS: 71-23-8    │
│ [查看详情]      │  │ [查看详情]      │
└─────────────────┘  └─────────────────┘

┌─────────────────────────────────────────┐
│ 共存禁忌分析                              │
├─────────────────────────────────────────┤
│ ✅ 安全评估：可以共存                     │
│ ⚠️  风险等级：中等                        │
│                                          │
│ 注意事项：                                │
│ • 两者均为易燃液体，注意防火             │
│ • 建议分开存储                            │
└─────────────────────────────────────────┘
```

---

## 📝 实施步骤（详细）

### 第1周：多化学品查询基础功能

**Day 1-2：后端API开发**
- [ ] 新增 `POST /api/search-multiple` 接口
- [ ] 实现多关键词解析和批量查询
- [ ] 测试API返回格式

**Day 3-4：前端界面修改**
- [ ] 修改搜索框支持多关键词输入
- [ ] 实现关键词解析（支持多种分隔符）
- [ ] 修改搜索结果展示，支持多个化学品卡片

**Day 5：联调测试**
- [ ] 测试多化学品查询功能
- [ ] 修复bug

### 第2周：文本解析模块

**Day 1-2：文本解析算法**
- [ ] 实现 `extract_incompatible_substances()` 函数
- [ ] 测试解析准确性
- [ ] 优化正则表达式匹配

**Day 3-4：化学品名称匹配**
- [ ] 实现 `match_substance_to_chemical()` 函数
- [ ] 实现类别匹配（强氧化剂、强酸等）
- [ ] 测试匹配准确性

**Day 5：数据验证**
- [ ] 抽取10-20个化学品样本验证解析结果
- [ ] 建立测试用例

### 第3周：共存禁忌分析

**Day 1-2：核心分析逻辑**
- [ ] 实现 `POST /api/compatibility-check` 接口
- [ ] 实现交叉比对算法
- [ ] 返回冲突列表

**Day 3-4：前端展示**
- [ ] 设计共存分析结果UI
- [ ] 实现风险等级可视化
- [ ] 显示禁忌组合列表

**Day 5：测试优化**
- [ ] 测试各种化学品组合
- [ ] 优化算法性能

### 第4周：危害评估

**Day 1-2：危害评估算法**
- [ ] 实现 `assess_hazard_level()` 函数
- [ ] 实现GHS分类叠加分析
- [ ] 实现反应性风险评估

**Day 3-4：报告生成**
- [ ] 生成风险评估报告
- [ ] 生成建议措施
- [ ] 前端展示完整报告

**Day 5：整体测试**
- [ ] 端到端测试
- [ ] 性能优化
- [ ] 文档编写

---

## ⚠️ 技术挑战与解决方案

### 挑战1：文本解析准确性

**问题：**
- MSDS文本格式不统一
- 化学品名称表述多样

**解决方案：**
- 建立化学品名称同义词库
- 使用多个正则表达式模式
- 人工审核+机器学习改进

### 挑战2：类别匹配

**问题：**
- "强氧化剂"是类别，不是具体化学品
- 需要判断哪些化学品属于该类别

**解决方案：**
- 建立类别映射表
- 基于GHS分类判断
- 结合MSDS第2章信息

### 挑战3：性能优化

**问题：**
- 多化学品查询需要多次数据库访问
- 文本解析可能较慢

**解决方案：**
- 使用批量查询优化SQL
- 缓存解析结果
- 异步处理

---

## 📈 后续优化方向

### 短期（1-3个月）
1. **化学品名称同义词库**
   - 建立常用化学品别名映射
   - 提高匹配准确率

2. **反应规则知识库**
   - 从MSDS中提取更多反应规则
   - 人工审核和补充

3. **更精确的危害评估**
   - 引入更多评估维度
   - 基于历史事故数据

### 长期（3-6个月）
1. **机器学习优化**
   - 训练模型识别不相容物质
   - 自动提取反应规则

2. **外部数据集成**
   - 接入化学品反应数据库
   - 整合行业标准

3. **实时监控**
   - 化学品存储位置监控
   - 自动预警系统

---

## 🎯 成功标准

### 功能完整性
- ✅ 支持2-5个化学品同时查询
- ✅ 准确识别80%以上的不相容组合
- ✅ 生成清晰的风险评估报告

### 性能指标
- ⚡ 多化学品查询响应时间 < 2秒
- ⚡ 兼容性分析响应时间 < 3秒

### 用户体验
- 🎨 界面直观易用
- 📱 支持移动端访问
- 📊 报告清晰易懂

---

## 📚 参考资料

1. **MSDS标准**
   - GB/T 16483-2008 化学品安全技术说明书
   - 第10章：稳定性和反应性

2. **化学品分类**
   - GHS全球化学品统一分类和标签制度
   - 联合国危险货物运输建议书

3. **相关数据库**
   - 化学品反应数据库（需调研）
   - 事故案例库（需调研）

---

## ✅ 总结

**可行性评估：** ⭐⭐⭐⭐ (4/5)

**推荐方案：** 方案A（基于MSDS文本解析）

**预计开发周期：** 4-6周

**主要优势：**
- 利用现有数据，无需额外数据源
- 实现周期短，可快速上线
- 后续可逐步优化和扩展

**主要风险：**
- 文本解析准确性需要持续优化
- 可能需要人工审核和维护

---

**文档版本：** 1.0  
**创建日期：** 2025-10-24  
**最后更新：** 2025-10-24


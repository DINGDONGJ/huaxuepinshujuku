# 危化品大模型数据库 - 设计文档

> **版本**: 1.0  
> **创建日期**: 2025-10-09  
> **数据库**: MySQL 8.0+  
> **字符集**: utf8mb4_0900_ai_ci  
> **基于**: 《危化品大模型数据库设计.md》方案

---

## 📋 目录

1. [系统概述](#1-系统概述)
2. [总体架构](#2-总体架构)
3. [数据表设计](#3-数据表设计)
4. [索引策略](#4-索引策略)
5. [查询性能优化](#5-查询性能优化)
6. [数据关系图](#6-数据关系图)
7. [字段说明](#7-字段说明)

---

## 1. 系统概述

### 1.1 系统目标

构建支持**"自然语言 → 一键输出合规清单"**的危化品数据库系统，实现：

- 🔍 **智能检索**: 支持中文名/英文名/别名/CAS号的模糊和全文检索
- 📋 **四块卡片输出**:
  1. 管理要求（法规条款）
  2. 使用要求（SOP + PPE）
  3. 识别与许可（UN编号、运输分类、目录标识）
  4. 应急措施（急救、消防、泄漏处置）
- 📚 **数据整合**: MSDS 16部分 + 法律法规 + 操作规程
- 🌍 **地域化支持**: 支持省市级法规差异

### 1.2 技术规格

| 项目 | 规格 |
|------|------|
| 数据库 | MySQL 8.0+ |
| 字符集 | utf8mb4 |
| 排序规则 | utf8mb4_0900_ai_ci |
| 时区 | UTC |
| 存储引擎 | InnoDB |
| 主键类型 | BIGINT AUTO_INCREMENT |
| 全文检索 | InnoDB FULLTEXT |

---

## 2. 总体架构

### 2.1 五层架构设计

```
┌─────────────────────────────────────────────────────────┐
│  E层: 自然语言检索增强                                     │
│  - nl_facets (四块分类)                                   │
│  - nl_template (提示词模板)                               │
│  - FULLTEXT索引 (names_synonyms, clauses, msds_sections) │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│  D层: MSDS数据（16部分）                                   │
│  - msds_headers (MSDS文档头)                             │
│  - msds_sections (16个章节)                              │
│  - msds_physchem (理化特性外提)                           │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│  C层: 映射与可拼装单元                                     │
│  - chem_clause_map (化学品↔条款映射，含相关度评分)        │
│  - sop_library + sop_steps (标准操作规程)                │
│  - emergency_cards (应急卡片)                            │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│  B层: 文档与条款管理                                       │
│  - agencies (监管机构)                                    │
│  - documents + document_versions (法规文档+版本化)       │
│  - clauses (条款，含facet分类)                           │
│  - doc_jurisdictions (地域管辖)                          │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│  A层: 化学品主数据                                         │
│  - chemicals (化学品基本信息)                             │
│  - names_synonyms (中英文别名/同义词)                     │
│  - ghs_classifications (GHS分类与标签)                   │
│  - transport_classifications (运输分类/UN编号)           │
│  - hazard_catalog_flags (法规名录列入情况)               │
└─────────────────────────────────────────────────────────┘
```

### 2.2 数据流向

```
用户输入 "甲醛"
    ↓
[E层] 全文检索 names_synonyms → 获取 chemical_id
    ↓
[C层] 查询 chem_clause_map → 获取相关条款ID列表
    ↓
[B层] 查询 clauses + documents → 组装管理要求
    ↓
[C层] 查询 sop_library → 组装使用要求
    ↓
[A层] 查询 transport_classifications + hazard_catalog_flags → 组装识别信息
    ↓
[D层] 查询 msds_sections (section_no IN (4,5,6)) → 组装应急措施
    ↓
[输出] JSON格式四块卡片
```

---

## 3. 数据表设计

### 3.1 A层：化学品主数据（5张表）

#### 3.1.1 chemicals（化学品主表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AI | 化学品ID |
| cas_no | VARCHAR(32) | NOT NULL, UNIQUE | CAS号（唯一标识） |
| cn_name | VARCHAR(256) | NOT NULL | 中文名称 |
| en_name | VARCHAR(256) | | 英文名称 |
| formula | VARCHAR(64) | | 分子式 |
| ec_no | VARCHAR(64) | | EC编号 |
| is_active | TINYINT | DEFAULT 1 | 有效状态 |
| notes | TEXT | | 备注 |
| created_at | TIMESTAMP | | 创建时间 |
| updated_at | TIMESTAMP | | 更新时间 |

**索引**:
- PRIMARY KEY (id)
- UNIQUE KEY (cas_no)
- INDEX (cn_name(191))
- INDEX (en_name(191))
- INDEX (is_active)

#### 3.1.2 names_synonyms（名称/同义词表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AI | 同义词ID |
| chemical_id | BIGINT | FK, NOT NULL | 化学品ID |
| name | VARCHAR(256) | NOT NULL | 别名/同义词 |
| lang | ENUM | DEFAULT 'zh' | 语言类型（zh/en/other） |
| is_primary | TINYINT | DEFAULT 0 | 是否主要名称 |
| created_at | TIMESTAMP | | 创建时间 |

**索引**:
- PRIMARY KEY (id)
- INDEX (chemical_id)
- **FULLTEXT (name)** ← 支持全文检索
- FOREIGN KEY (chemical_id) → chemicals(id)

#### 3.1.3 ghs_classifications（GHS分类表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AI | 分类ID |
| chemical_id | BIGINT | FK, NOT NULL | 化学品ID |
| hazard_class | VARCHAR(128) | NOT NULL | 危害类别 |
| category | VARCHAR(64) | | 类别编号 |
| signal_word | VARCHAR(32) | | 信号词（危险/警告） |
| pictograms | JSON | | 象形图代码数组 |
| h_codes | JSON | | H语句代码数组 |
| p_codes | JSON | | P语句代码数组 |
| source | VARCHAR(128) | | 数据来源 |

**索引**:
- PRIMARY KEY (id)
- INDEX (chemical_id)
- FOREIGN KEY (chemical_id) → chemicals(id)

#### 3.1.4 transport_classifications（运输分类表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AI | 运输分类ID |
| chemical_id | BIGINT | FK, NOT NULL | 化学品ID |
| un_number | VARCHAR(16) | | UN编号 |
| un_proper_name | VARCHAR(256) | | UN运输正式名称 |
| primary_class | VARCHAR(64) | | 主要危险类别 |
| secondary_class | VARCHAR(64) | | 次要危险类别 |
| packing_group | VARCHAR(8) | | 包装类别（I/II/III） |
| marine_pollutant | TINYINT | | 是否海洋污染物 |
| remarks | TEXT | | 运输注意事项 |

**索引**:
- PRIMARY KEY (id)
- INDEX (chemical_id)
- INDEX (un_number)

#### 3.1.5 hazard_catalog_flags（危险品目录标识表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AI | 标识ID |
| chemical_id | BIGINT | FK, NOT NULL | 化学品ID |
| catalog_key | VARCHAR(128) | NOT NULL | 目录标识 |
| is_listed | TINYINT | NOT NULL | 是否列入 |
| reference | VARCHAR(256) | | 公告/文号 |

**索引**:
- PRIMARY KEY (id)
- INDEX (chemical_id)
- UNIQUE KEY (chemical_id, catalog_key)

---

### 3.2 B层：文档与条款管理（5张表）

#### 3.2.1 agencies（监管机构表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK AI | 机构ID |
| name | VARCHAR(256) | 机构名称 |
| country_region | VARCHAR(128) | 国家/地区 |
| url | VARCHAR(512) | 官方网站 |

#### 3.2.2 documents（文档表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AI | 文档ID |
| doc_type | ENUM | NOT NULL | 文档类型（LAW/REGULATION/STANDARD） |
| title | VARCHAR(512) | NOT NULL | 文档标题 |
| short_title | VARCHAR(256) | | 简称 |
| issuer_agency_id | BIGINT | FK | 发布机构ID |
| publish_date | DATE | | 发布日期 |
| effective_date | DATE | | 生效日期 |
| status | ENUM | DEFAULT 'EFFECTIVE' | 状态（DRAFT/EFFECTIVE/REPEALED） |
| official_link | VARCHAR(1024) | | 官方链接 |
| file_uri | VARCHAR(1024) | | 文件存储地址 |
| checksum | VARCHAR(128) | | 文件校验值 |

**索引**:
- PRIMARY KEY (id)
- INDEX (doc_type, effective_date)
- INDEX (title(191))
- INDEX (status)

#### 3.2.3 doc_jurisdictions（文档地域管辖表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK AI | 地域管辖ID |
| document_id | BIGINT FK | 文档ID |
| country | VARCHAR(128) | 国家 |
| province | VARCHAR(128) | 省份 |
| city | VARCHAR(128) | 城市 |
| scope_note | VARCHAR(256) | 适用范围说明 |

**索引**:
- INDEX (document_id)
- INDEX (country, province, city)

#### 3.2.4 document_versions（文档版本表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK AI | 版本ID |
| document_id | BIGINT FK | 文档ID |
| version_label | VARCHAR(64) | 版本标签 |
| issue_date | DATE | 发布日期 |
| is_current | TINYINT | 是否当前版本 |
| change_log | TEXT | 变更日志 |
| file_uri | VARCHAR(1024) | 文件存储地址 |

**索引**:
- INDEX (document_id)
- INDEX (is_current)

#### 3.2.5 clauses（条款表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AI | 条款ID |
| document_version_id | BIGINT | FK, NOT NULL | 文档版本ID |
| clause_no | VARCHAR(64) | | 条款编号 |
| heading | VARCHAR(512) | | 条款标题 |
| text | LONGTEXT | NOT NULL | 条文全文 |
| facet | ENUM | DEFAULT 'OTHER' | 条款分类 |
| anchor_link | VARCHAR(1024) | | PDF锚点链接 |

**facet枚举值**:
- `MANAGEMENT` - 管理要求
- `USE_SOP` - 使用要求
- `IDENTIFICATION_PERMIT` - 识别与许可
- `EMERGENCY` - 应急措施
- `OTHER` - 其他

**索引**:
- PRIMARY KEY (id)
- INDEX (document_version_id)
- INDEX (facet)
- **FULLTEXT (heading, text)** ← 支持全文检索

---

### 3.3 C层：映射与可拼装单元（4张表）

#### 3.3.1 chem_clause_map（化学品-条款映射表）⭐核心

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AI | 映射ID |
| chemical_id | BIGINT | FK, NOT NULL | 化学品ID |
| clause_id | BIGINT | FK, NOT NULL | 条款ID |
| relevance_score | DECIMAL(5,2) | DEFAULT 1.00 | 相关度评分（0.00-99.99） |
| tag | VARCHAR(64) | | 标签（如：许可、储存、运输） |

**索引**:
- PRIMARY KEY (id)
- **UNIQUE KEY (chemical_id, clause_id)**
- INDEX (chemical_id)
- INDEX (clause_id)
- INDEX (relevance_score DESC)

**重要性**: 此表是实现"自然语言→精确条款"的关键！

#### 3.3.2 sop_library（SOP标准操作规程库）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK AI | SOP ID |
| title | VARCHAR(256) | SOP名称 |
| chemical_id | BIGINT FK | 化学品ID（NULL=通用SOP） |
| scope | ENUM | 适用范围 |
| version_label | VARCHAR(64) | 版本号 |
| body | LONGTEXT | SOP正文 |

**scope枚举值**:
- `STORAGE` - 储存
- `HANDLING` - 操作
- `DISPOSAL` - 处置
- `PPE` - 个人防护
- `VENTILATION` - 通风
- `OTHER` - 其他

#### 3.3.3 sop_steps（SOP步骤表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK AI | 步骤ID |
| sop_id | BIGINT FK | SOP ID |
| step_no | INT | 步骤序号 |
| step_text | TEXT | 步骤内容 |

**索引**: INDEX (sop_id, step_no)

#### 3.3.4 emergency_cards（应急卡片表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK AI | 应急卡片ID |
| chemical_id | BIGINT FK | 化学品ID（NULL=通用） |
| section | ENUM | 应急类型 |
| body | LONGTEXT | 应急措施内容 |
| version_label | VARCHAR(64) | 版本号 |

**section枚举值**:
- `FIRST_AID` - 急救措施
- `FIRE_FIGHTING` - 消防措施
- `SPILL_RESPONSE` - 泄漏处置

---

### 3.4 D层：MSDS数据（3张表）

#### 3.4.1 msds_headers（MSDS文档头表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AI | MSDS文档ID |
| chemical_id | BIGINT | FK, UNIQUE | 化学品ID（一对一） |
| provider | VARCHAR(256) | | 提供者/编制单位 |
| provider_contact | JSON | | 提供者联系方式 |
| compiled_by | VARCHAR(256) | | 编制人 |
| compiled_at | DATE | | 编制日期 |
| standard_basis | VARCHAR(256) | | 编制依据标准 |

#### 3.4.2 msds_sections（MSDS章节表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AI | 章节ID |
| msds_id | BIGINT | FK, NOT NULL | MSDS文档ID |
| section_no | TINYINT | NOT NULL | 章节号（1-16） |
| section_title | VARCHAR(256) | NOT NULL | 章节标题 |
| content | LONGTEXT | | 章节原文 |
| content_json | JSON | | 结构化内容 |

**MSDS 16部分对照**:
1. 化学品及企业标识
2. 危险性概述
3. 成分/组成信息
4. **急救措施** ← 应急
5. **消防措施** ← 应急
6. **泄漏应急处理** ← 应急
7. 操作处置与储存
8. **接触控制/个体防护** ← 使用要求
9. 理化特性
10. 稳定性和反应性
11. 毒理学信息
12. 生态学信息
13. 废弃处置
14. 运输信息
15. 法规信息
16. 其他信息

**索引**:
- PRIMARY KEY (id)
- UNIQUE KEY (msds_id, section_no)
- INDEX (section_no)
- **FULLTEXT (section_title, content)**

#### 3.4.3 msds_physchem（MSDS理化特性表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK AI | 理化特性ID |
| msds_id | BIGINT FK | MSDS文档ID |
| appearance | VARCHAR(256) | 外观 |
| ph | VARCHAR(64) | pH值 |
| melting_point | VARCHAR(64) | 熔点 |
| boiling_point | VARCHAR(64) | 沸点/初沸点 |
| flash_point | VARCHAR(64) | 闪点 |
| flammability | VARCHAR(64) | 易燃性 |
| explosive_limits | VARCHAR(64) | 爆炸极限 |
| vapor_pressure | VARCHAR(64) | 蒸气压 |
| density | VARCHAR(64) | 相对密度 |
| solubility | VARCHAR(128) | 溶解性 |
| auto_ignition_temp | VARCHAR(64) | 自燃温度 |

**说明**: 将MSDS第9部分的关键参数外提，便于快速查询和比较。

---

### 3.5 E层：自然语言检索增强（2张表）

#### 3.5.1 nl_facets（自然语言分面表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK AI | 分面ID |
| facet_key | ENUM | 分面键（MANAGEMENT/USE_SOP/IDENTIFICATION_PERMIT/EMERGENCY） |
| display_name | VARCHAR(128) | 展示名称 |

**初始数据**:
```sql
INSERT INTO nl_facets VALUES
    (1, 'MANAGEMENT', '管理要求'),
    (2, 'USE_SOP', '使用要求'),
    (3, 'IDENTIFICATION_PERMIT', '识别与许可'),
    (4, 'EMERGENCY', '应急措施');
```

#### 3.5.2 nl_template（自然语言模板表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK AI | 模板ID |
| facet_key | ENUM | 分面键 |
| template_text | VARCHAR(512) | 模板文本 |

**模板示例**:
```
<NAME> 管理要求 法律 法规 条款
<NAME> 使用 操作规程 防护 PPE
<NAME> 识别 运输 许可 UN编号
<NAME> 应急 急救 消防 泄漏
```

---

## 4. 索引策略

### 4.1 主键索引（15张表）

所有表的主键均为 `BIGINT AUTO_INCREMENT`，自动建立聚簇索引。

### 4.2 外键索引（14个外键关系）

| 子表 | 外键字段 | 父表 | 级联策略 |
|------|---------|------|---------|
| names_synonyms | chemical_id | chemicals | CASCADE |
| ghs_classifications | chemical_id | chemicals | CASCADE |
| transport_classifications | chemical_id | chemicals | CASCADE |
| hazard_catalog_flags | chemical_id | chemicals | CASCADE |
| documents | issuer_agency_id | agencies | SET NULL |
| doc_jurisdictions | document_id | documents | CASCADE |
| document_versions | document_id | documents | CASCADE |
| clauses | document_version_id | document_versions | CASCADE |
| chem_clause_map | chemical_id | chemicals | CASCADE |
| chem_clause_map | clause_id | clauses | CASCADE |
| sop_library | chemical_id | chemicals | CASCADE |
| sop_steps | sop_id | sop_library | CASCADE |
| emergency_cards | chemical_id | chemicals | CASCADE |
| msds_headers | chemical_id | chemicals | CASCADE |
| msds_sections | msds_id | msds_headers | CASCADE |
| msds_physchem | msds_id | msds_headers | CASCADE |

### 4.3 FULLTEXT全文索引（3个）

| 表名 | 字段 | 用途 |
|------|------|------|
| names_synonyms | (name) | 化学品名称/别名检索 |
| clauses | (heading, text) | 法规条款内容检索 |
| msds_sections | (section_title, content) | MSDS章节内容检索 |

**使用方式**:
```sql
-- 自然语言模式
WHERE MATCH(name) AGAINST('乙醇 酒精' IN NATURAL LANGUAGE MODE)

-- 布尔模式
WHERE MATCH(heading, text) AGAINST('+甲醛 +储存 -废弃' IN BOOLEAN MODE)

-- 查询扩展
WHERE MATCH(text) AGAINST('危险化学品' WITH QUERY EXPANSION)
```

### 4.4 组合索引（关键）

| 表名 | 索引 | 用途 |
|------|------|------|
| documents | (doc_type, effective_date) | 按类型和日期查询 |
| doc_jurisdictions | (country, province, city) | 地域查询 |
| msds_sections | (msds_id, section_no) | 快速定位MSDS章节 |
| sop_steps | (sop_id, step_no) | 按序号获取SOP步骤 |

### 4.5 唯一索引（4个）

| 表名 | 字段 | 说明 |
|------|------|------|
| chemicals | (cas_no) | CAS号唯一性 |
| hazard_catalog_flags | (chemical_id, catalog_key) | 防止重复标识 |
| chem_clause_map | (chemical_id, clause_id) | 防止重复映射 |
| msds_sections | (msds_id, section_no) | 每个MSDS只有一个章节号 |

---

## 5. 查询性能优化

### 5.1 查询优化原则

1. **避免SELECT ***：明确指定需要的字段
2. **利用覆盖索引**：查询字段完全包含在索引中
3. **小表驱动大表**：JOIN时小表在前
4. **分页查询加LIMIT**：避免全表扫描
5. **使用EXPLAIN分析**：检查执行计划

### 5.2 高频查询优化

#### 查询1：通过名称检索化学品

**优化前**:
```sql
SELECT * FROM chemicals 
WHERE cn_name LIKE '%甲醛%';
```

**优化后**:
```sql
-- 使用FULLTEXT索引
SELECT c.id, c.cas_no, c.cn_name, c.en_name
FROM chemicals c
JOIN names_synonyms ns ON c.id = ns.chemical_id
WHERE MATCH(ns.name) AGAINST('甲醛' IN NATURAL LANGUAGE MODE)
LIMIT 10;
```

**性能提升**: 10x-100x（取决于数据量）

#### 查询2：获取化学品的法规条款

**优化策略**:
- 使用`relevance_score DESC`索引
- 过滤条件`is_current = 1`
- 限制返回数量`LIMIT 20`

```sql
SELECT 
    d.title, cl.clause_no, cl.heading,
    ccm.relevance_score
FROM chem_clause_map ccm
    USE INDEX (idx_ccm_chem)  -- 强制使用索引
JOIN clauses cl ON cl.id = ccm.clause_id
JOIN document_versions dv USE INDEX (idx_docver_current) 
    ON dv.id = cl.document_version_id
JOIN documents d ON d.id = dv.document_id
WHERE ccm.chemical_id = ?
  AND dv.is_current = 1
  AND d.status = 'EFFECTIVE'
ORDER BY ccm.relevance_score DESC
LIMIT 20;
```

#### 查询3：获取MSDS特定章节

**优化**:
- 使用UNIQUE索引`(msds_id, section_no)`
- 直接定位，无需排序

```sql
SELECT content
FROM msds_sections
WHERE msds_id = ? AND section_no IN (4,5,6);
```

### 5.3 缓存策略

**推荐使用Redis缓存**:

1. **化学品基本信息**: TTL 24小时
   ```
   Key: chem:{cas_no}
   Value: JSON(id, cn_name, en_name, formula)
   ```

2. **四块卡片完整数据**: TTL 6小时
   ```
   Key: cards:{chemical_id}
   Value: JSON(management, use, identification, emergency)
   ```

3. **热门化学品列表**: TTL 1小时
   ```
   Key: hot_chemicals
   Value: LIST[chemical_ids]
   ```

---

## 6. 数据关系图

### 6.1 ER关系图（简化版）

```
┌─────────────┐
│  chemicals  │ (主表)
└──────┬──────┘
       │ 1
       │
       ├───────* names_synonyms (别名)
       ├───────* ghs_classifications (GHS分类)
       ├───────* transport_classifications (运输分类)
       ├───────* hazard_catalog_flags (目录标识)
       │
       ├───────1 msds_headers ───1───* msds_sections (MSDS 16部分)
       │                        └───1 msds_physchem (理化特性)
       │
       ├───────* sop_library ───1───* sop_steps (SOP步骤)
       ├───────* emergency_cards (应急卡)
       │
       └───────* chem_clause_map (映射) ───* clauses ───1 document_versions ───1 documents
                                                                                    │
                                                                                    ├───1 agencies
                                                                                    └───* doc_jurisdictions
```

### 6.2 核心映射关系

```
chemicals (化学品)
    ↕ (N:M)
chem_clause_map (映射表，含relevance_score)
    ↕ (N:M)
clauses (条款，含facet分类)
    ↕ (N:1)
document_versions (版本，is_current)
    ↕ (N:1)
documents (文档，doc_type)
```

---

## 7. 字段说明

### 7.1 关键枚举类型

#### doc_type (文档类型)
- `LAW` - 法律
- `REGULATION` - 条例/法规
- `STANDARD` - 标准

#### status (文档状态)
- `DRAFT` - 草稿
- `EFFECTIVE` - 有效
- `REPEALED` - 废止

#### facet (条款分类)
- `MANAGEMENT` - 管理要求
- `USE_SOP` - 使用要求
- `IDENTIFICATION_PERMIT` - 识别与许可
- `EMERGENCY` - 应急措施
- `OTHER` - 其他

### 7.2 JSON字段格式

#### ghs_classifications.pictograms
```json
["GHS02", "GHS07", "GHS08"]
```

#### ghs_classifications.h_codes
```json
["H225", "H319", "H336"]
```

#### msds_headers.provider_contact
```json
{
  "phone": "0519-85150306",
  "email": "info@example.com",
  "address": "江苏省常州市"
}
```

#### msds_sections.content_json
```json
{
  "cas": "64-17-5",
  "formula": "C2H6O",
  "molecular_weight": 46.07,
  "h_codes": ["H225"],
  "pictograms": ["GHS02"]
}
```

---

## 附录A：数据库文件清单

### SQL脚本文件

| 文件名 | 说明 | 行数 |
|--------|------|------|
| `init_database.sql` | 数据库初始化（建表DDL） | ~600 |
| `sample_data.sql` | 示例数据（甲醛、乙醇MSDS） | ~400 |
| `regulation_data.sql` | 法规条款和映射关系 | ~500 |
| `query_examples.sql` | 核心查询示例 | ~450 |

### 执行顺序

```bash
mysql -u root -p < db/init_database.sql
mysql -u root -p hazardous_chemicals_db < db/sample_data.sql
mysql -u root -p hazardous_chemicals_db < db/regulation_data.sql
```

---

## 附录B：版本历史

| 版本 | 日期 | 修改内容 |
|------|------|---------|
| 1.0 | 2025-10-09 | 初始版本，完成A-E层设计 |

---

**文档维护者**: AI助手  
**最后更新**: 2025-10-09  
**文档状态**: ✅ 已完成



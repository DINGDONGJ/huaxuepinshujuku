正是把“自然语言  $\rightarrow$  一键输出合规清单”落地成系统的典型场景。下面可直接落库到MySQL的详尽数据库设计方案（含表结构、索引、关系、示例SQL、扩展策略），围绕你要的三大类文档（法律/条例/标准）与MSDS数据、化学品主数据、以及面向“甲醛”这类自然语言查询时的一键组装能力来构建。

说明：我参考了你提供的小程序页面与 MSDS 结构（16 部分），并抽取为结构化字段与从属表，以支持检索、拼装与问答生成。

# 1. 总体目标与查询产物（面向“甲醛”之类自然语言输入）

输入：“甲醛” (或CAS、同义词、英文名)

输出（直接用于前端展示/调用）：

1. 管理要求：匹配的法律/条例/标准条款（含条号、关键句、原文链接）  
2. 使用要求：安全操作规程/SOP与接触控制/个体防护（来自MSDS+内部规程库）  
3.识别/运输许可与分类：危险性类别、UN/类别、准运/许可证级别、是否剧毒/易制爆/重点监管等  
4. 应急措施：急救/消防/泄漏处置(MSDS第4、5、6部分)  
5. 辅助：标签要素（GHS/象形图/信号词/H-短语/P-短语）、理化特性、法规目录列入情况等

# 2. 逻辑分层与ER关系

- A层：主数据：chemicals（化学品），names_synonyms（中英文别名/同义词），ghs classifications (GHS分类与标签）、transport_classifications（运输分类/UN/包材级别），hazard_catalog_flags（法规名录列入与否）等  
- B层：文档与条款（三大类合并为统一“文档—条款”模型 + 版本化）：documents、document Versions、clauses、doc_jurisdictions、agencies  
• C层：映射与可拼装单元：将化学品  $\leftrightarrow$  条款、化学品  $\leftrightarrow$  操作规程/应急卡片建立映射，用于“自然语言结果拼装”：chem_clause_map 、 sob_library 、 SOP_steps 、 emergency Cards  
- D层：MSDS 数据（16 部分拆分为主表 + 从表/JSON 字段），供 B/C 层补全  
- E层：自然语言检索增强：同义词、引导词模板、全文索引与权重：nl_template 、 nl_facets 、 names_synonyms 的 FULLTEXT、clauses 的 FULLTEXT

# ER (ASCII) 概览

```txt
1 [chemicals] 1---* [names_synonyms]  
2 | |  
3 | |  
4 | *---* [chem_clause_map] *---* [clauses] *---1 [document Versions] *---1  
[documents]  
5 | |
```

```txt
6 | ----* [doc_jurisdictions] ----1  
[agencies]  
7 |  
8 1---1 [msds Headers] 1---1 [msds_sections] (16部分可JSON或分表)  
9 |  
10 1---* [ghs_classifications]  
11 1---* [transport_classifications]  
12 1---* [hazard_catalog_flags]  
13  
14 [sop_library] 1---* [sop_steps]  
15 [emergency Cards] --(与 chemicals 多对多映射或基于 CAS 规则推断)  
16 [nl_template], [nl_facets] --(提示词拼装)  
17
```

# 3. MySQL 表设计 (DDL 重点)

字符集统一建议：utf8mb4 + utf8mb4_0900.ai_ci；所有时间：TIMESTAMP (UTC) + created_by/updated_by 便于审计；主键均为BIGINT AUTO_INCREMENT。

# 3.1 化学品主数据

```sql
1 CREATE TABLE chemicals (
2 id BIGINT PRIMARY KEY AUTO_INCREMENT,
3 cas_no VARCHAR(32) NOT NULL UNIQUE, -- e.g., 64-17-5
4 cn_name VARCHAR(256) NOT NULL, -- 中文名称（主显示名）
5 en_name VARCHAR(256), -- 英文主名
6 formula VARCHAR(64), -- 分子式
7 ec_no VARCHAR(64), -- EC NO.
8 is.active TINYINT DEFAULT 1,
9 notes TEXT,
10 created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
11 updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
12 );
13
14 CREATE TABLE names_synonyms (
15 id BIGINT PRIMARY KEY AUTO_INCREMENT,
16 chemical_id BIGINT NOT NULL,
17 name VARCHAR(256) NOT NULL, -- 别名/同义词（中英混合）
```

```txt
18 langENUM('zh','en','other')DEFAULT'zh',   
19 is_primary TINYINT DEFAULT 0,   
20 INDEX idx_ns_chem (chemical_id),   
21 FULLTEXT INDEX ftx_ns_name (name),   
22 CONSTRAINTfk_ns_chemFOREIGNKEY(chemical_id)REFERENCESchemicals(id)   
23);   
24
```

# 3.2 GHS与运输分类、法规名录标识

```sql
1 CREATE TABLE ghs classifications (  
2 id BIGINT PRIMARY KEY AUTO_INCREMENT,  
3 chemical_id BIGINT NOT NULL,  
4 hazard_classVARCHAR(128) NOT NULL, -- 如：易燃液体  
5 category VARCHAR(64), -- 类别2/3等  
6 signal_word VARCHAR(32), -- 危险/警告  
7 pictograms JSON, -- 存储象形图代码数组  
8 h_codes JSON, -- ["H225", ...]  
9 p_codes JSON, -- ["P210","P233", ...]  
10 source VARCHAR(128), -- 来源（MSDS/标准）  
11 INDEX idx_ghs_chem (chemical_id),  
12 CONSTRAINTfk_ghs_chem FOREIGN KEY (chemical_id) REFERENCES chemicals(id)  
13);  
14  
15 CREATE TABLE transport_classifications (  
16 id BIGINT PRIMARY KEY AUTO_INCREMENT,  
17 chemical_id BIGINT NOT NULL,  
18 un_number VARCHAR(16), -- UN编号  
19 primary_class VARCHAR(64), -- 主要危险类别（如3）  
20 secondary_class VARCHAR(64), -- 次要类别  
21 packing_group VARCHAR(8), -- 包装类别 I/II/III  
22 marine POLLUTANT TINYINT, -- 是否海污  
23 remarks TEXT, -- 运输注意事项摘要  
24 INDEX idx_tr_chem (chemical_id),  
25 CONSTRAINTfk_tr_chem FOREIGN KEY (chemical_id) REFERENCES chemicals(id)  
26);  
27  
28 CREATE TABLE hazard_catalog_flags (  
29 id BIGINT PRIMARY KEY AUTO_INCREMENT,
```

```txt
30 chemical_id BIGINT NOT NULL,  
31 catalog_key VARCHAR(128) NOT NULL, -- 目录标识，如"危化品目录2015", "易制爆2011", "重点监管"  
32 is Listed TINYINT NOT NULL, -- 1列入/0未列入  
33 reference VARCHAR(256), -- 公告/文号  
34 INDEX idx_flag_chem (chemical_id),  
35 UNIQUE KEY uq_catalog (chemical_id, catalog_key),  
36 CONSTRAINTfk_flag_chemFOREIGN KEY (chemical_id) REFERENCES chemicals(id)  
37);  
38
```

# 3.3 文档（法律/条例/标准）统一建模 + 版本化 + 条款化

```sql
1 CREATE TABLE documents (
2 id BIGINT PRIMARY KEY AUTO_INCREMENT,
3 doc_type ENUM('LAW','REGULATION','STANDARD') NOT NULL,
4 titleVARCHAR(512) NOT NULL,
5 short_title VARCHAR(256),
6 issuer_agency_id BIGINT, -- 发布机构
7 publish_date DATE,
8 effective_date DATE,
9 status ENUM('DRAFT','EFFECTIVE','REPEALED') DEFAULT 'EFFECTIVE',
10 official_link VARCHAR(1024), -- 官方链接
11 fileUri VARCHAR(1024), -- 文件存储/对象存储地址
12 checksum VARCHAR(128), -- 校验
13 INDEX idx_doc_type_date (doc_type, effective_date),
14 INDEX idx_doc_title (title(191))
15 );
16
17 CREATE TABLE agencies (
18 id BIGINT PRIMARY KEY AUTO_INCREMENT,
19 name VARCHAR(256) NOT NULL, -- 监管/标准制定机构
20 country_region VARCHAR(128), -- 国家/地区
21 url VARCHAR(512)
22 );
23
24 ALTER TABLE documents
25 ADD CONSTRAINTfk_doc_agency FOREIGN KEY (issuer_agency_id) REFERENCES agencies(id);
26
```

```sql
27 CREATE TABLE doc_jurisdictions (
28 id BIGINT PRIMARY KEY AUTO_INCREMENT,
29 document_id BIGINT NOT NULL,
30 countryVARCHAR(128) NOT NULL,
31 provinceVARCHAR(128),
32 cityVARCHAR(128),
33 scope-noteVARCHAR(256),
34 INDEX idx_docjur_doc (document_id),
35 CONSTRAINTfk_docjur_doc FOREIGN KEY (document_id) REFERENCES documents(id)
36 );
37
38 CREATE TABLE document Versions (
39 id BIGINT PRIMARY KEY AUTO_INCREMENT,
40 document_id BIGINT NOT NULL,
41 version_labelVARCHAR(64) NOT NULL, -- 如 2023修订、v2.0
42 issue_date DATE,
43 is_current TINYINT DEFAULT 1,
44 change_log TEXT,
45 fileUriVARCHAR(1024),
46 INDEX idx_docver_doc (document_id),
47 CONSTRAINTfk_docver_doc FOREIGN KEY (document_id) REFERENCES documents(id)
48 );
49
50 CREATE TABLE clauses (
51 id BIGINT PRIMARY KEY AUTO_INCREMENT,
52 document_version_id BIGINT NOT NULL,
53 clause_noVARCHAR(64), -- 条/款/项编号
54 headingVARCHAR(512),
55 text LONGTEXT NOT NULL, -- 条文全文
56 facet ENUM('MANAGEMENT', 'USE_SOP', 'IDENTIFICATION_PERMIT', 'EMERGENCY', 'OTHER')
57 DEFAULT 'OTHER', -- 便于直连自然语言产出四块
58 anchor_linkVARCHAR(1024), -- 条内锚点链接
59 INDEX idx_clause_ver (document_version_id),
60 FULLTEXT INDEX fmtx_clause_text (heading, text),
61 CONSTRAINTfk_clause_ver FOREIGN KEY (document_version_id) REFERENCES documents(id)
62 );
63
```

# 3.4 化学品与条款映射（关键！）

```sql
1 CREATE TABLE chem_clause_map (
2 id BIGINT PRIMARY KEY AUTO_INCREMENT,
3 chemical_id BIGINT NOT NULL,
4 clause_id BIGINT NOT NULL,
5 relevance_score Decimal(5,2) DEFAULT 1.00, -- 相关度（人工或半自动）
6 tagVARCHAR(64), -- '许可', '储存', '运输', '限量'等
7 UNIQUE KEY uq_chem_clause (chemical_id, clause_id),
8 INDEX idx_ccm_chem (chemical_id),
9 CONSTRAINTfk_ccm_chemFOREIGN KEY (chemical_id) REFERENCES chemicals(id),
10 CONSTRAINTfk_ccm_clause FOREIGN KEY (clause_id) REFERENCES clauses(id)
);
```

这张表支撑“输入甲醛  $\rightarrow$  拉出精确条款与链接”。

# 3.5 SOP/应急卡片库（用于“使用要求/应急措施”富文本）

```sql
1 CREATE TABLE sob_library (
2 id BIGINT PRIMARY KEY AUTO_INCREMENT,
3 title VARCHAR(256) NOT NULL, -- SOP 名称（如：甲醛操作规程-配制/转运）
4 chemical_id BIGINT, -- 为空表示通用 SOP
5 scope ENUM('STORAGE','HANDLING','DISPOSAL','PPE','VENTILATION','OTHER') NOT NULL,
6 version_label VARCHAR(64),
7 body LONGTEXT, -- 可存 Markdown/HTML
8 INDEX idx_sop_chem (chemical_id),
9 CONSTRAINTfk_sop_chem FOREIGN KEY (chemical_id) REFERENCES chemicals(id)
10 );
11
12 CREATE TABLE sob_steps (
13 id BIGINT PRIMARY KEY AUTO_INCREMENT,
14 sop_id BIGINT NOT NULL,
15 step_no INT NOT NULL,
16 step_text TEXT NOT NULL,
17 CONSTRAINTfk_sop_step FOREIGN KEY (sop_id) REFERENCES soph.library(id),
18 INDEX idx_sop_step (sop_id, step_no)
19 );
```

```sql
20   
21 CREATE TABLE emergency Cards ( id BIGINT PRIMARY KEY AUTO_INCREMENT, chemical_id BIGINT, -- 专属某化学品或为空代表通用 section ENUM('FIRST_AID','FIRE_FIGHTING','SPILL_RESPONSE') NOT NULL, body LONGTEXT NOT NULL, version_labelVARCHAR(64), INDEX idx_emg_chem (chemical_id), CONSTRAINTfk_emg_chemFOREIGN KEY (chemical_id)REFERENCES chemicals(id) );   
30
```

# 3.6 MSDS (SDS) 结构化存储 (16 部分)

两种策略：

A) 分表：每个部分一张表（字段多、强结构化）  
B) 半结构: msds_sections 用 JSON 字段按 section_no 存储, 既可全文检索又灵活

建议用B+少量关键字段外提（便于快速查）：

```txt
1 CREATE TABLE msds Headers (  
2 id BIGINT PRIMARY KEY AUTO_INCREMENT,  
3 chemical_id BIGINT NOT NULL UNIQUE,  
4 provider VARCHAR(256), -- 提供者信息  
5 provider_contact JSON,  
6 compiled_by VARCHAR(256),  
7 compiled_at DATE,  
8 standard BASIS VARCHAR(256), -- 依照GB/T 17519、GB/T 16483等  
9 INDEX idx-msds_chem (chemical_id),  
10 CONSTRAINTfk-msds_chemFOREIGNKEY (chemical_id)REFERENCESchemicals(id)  
11);  
12  
13 CREATE TABLE msdsSections (  
14 id BIGINT PRIMARY KEY AUTO_INCREMENT,  
15 msds_id BIGINT NOT NULL,  
16 section_no TINYINT NOT NULL, -- 1..16  
17 section_title VARCHAR(256)NOTNULL, -- 如“急救措施”  
18 content LONGTEXT, -- 原文（HTML/Markdown）  
19 content_json JSON, -- 结构化抽取（如H/P代码、理化数值等）  
20 FULLTEXTINDEX ftx-msds_content (section_title, content),
```

关键字段外提示例（便于检索汇总）：  
```txt
UNIQUE KEY uq-msds_sec (msds_id, section_no),   
CONSTRAINTfk-msds_secFOREIGNKEY(msds_id)REFERENCESmsdsheaders(id)   
);   
24
```

```sql
1 CREATE TABLE msds_physchem (
2 id BIGINT PRIMARY KEY AUTO_INCREMENT,
3 msds_id BIGINT NOT NULL,
4 appearanceVARCHAR(256),
5 phVARCHAR(64),
6 melting_point VARCHAR(64),
7 boiling_point VARCHAR(64),
8 flash_point VARCHAR(64),
9 flammabilityVARCHAR(64),
10 explosiveLimits VARCHAR(64),
11 vapor_pressure VARCHAR(64),
12 density VARCHAR(64),
13 solubilityVARCHAR(128),
14 autoIgnition_temp VARCHAR(64),
15 INDEX idx-msds_phys (msds_id),
16 CONSTRAINTfk-msds_phys FOREIGN KEY (msds_id) REFERENCES msds Headers(id)
);
```

这些字段与MSDS的“第九部分理化特性”等对应，可按你提供的页面逐项抄录。

# 3.7 自然语言引导与检索增强

```sql
1 CREATE TABLE nl_facets (
2 id BIGINT PRIMARY KEY AUTO_INCREMENT,
3 facet_key ENUM('MANAGEMENT','USE_SOP','IDENTIFICATION_PERMIT','EMERGENCY') NOT NULL,
4 display_name VARCHAR(128) NOT NULL -- 展示名：管理要求/使用要求/识别与许可/应急措施
5);
6
7 CREATE TABLE nl_template (
8 id BIGINT PRIMARY KEY AUTO_INCREMENT,
```

```sql
9 facet_key ENUM('MANAGEMENT','USE_SOP','IDENTIFICATION_PERMIT','EMERGENCY') NOT NULL,  
10 template_textVARCHAR(512) NOT NULL -- 引导词模板，如："<NAME> 管理要求 法律 法规 条款"  
11);  
12
```

```txt
前端收到“甲醛”，可先通过 names_synonyms 找到主化学品，再按 nl_template 自动拼接检索提示词（用于全文搜索或召回加权），然后从 chem_clause_map + clauses、sop_library 、 emergency Cards 、 MsdsSections 组装四大块内容。
```

# 4. 关键索引与检索策略

- 名称/别名检索：

```txt
names_synonyms.ftx_ns_name + chemicals.cas_no 精确命中，返回 chemical_id
```

- 条款检索：

```txt
clauses.ftx_clause_text (可在 InnoDB FTS 上加 WITH QUERY EXPANSION ), 并叠加 chem_clause_map.relevance_score 排序
```

- MSDS 快速取数：

```txt
直接按msds_sections section_no IN (4,5,6,8,9)) 取“急救/消防/泄漏/个体防护/理化特性”
```

- 法规名录标签：

```txt
hazard_catalog_flags 将“是否列入重点监管/易制爆/危化品目录”等打平，便于“识别与许可”输出
```

# 5. 示例：最短路径从自然语言到结果

# 5.1 解析“甲醛”

```sql
1 -- 1）名称/别名/CAS 命中  
2 SELECT c.id, c.cas_no, c.cn_name, c.en_name  
3 FROM chemicals c  
4 LEFT JOIN names_synonyms s ON s.chemical_id = c.id  
5 WHERE c.cn_name = '甲醛'  
6 OR s.name = '甲醛'  
7 OR c.cas_no = '50-00-0'  
8 LIMIT 1;  
9
```

# 5.2 管理要求（法律/条例/标准条款）

```sql
1 SELECT d.doc_type, d.title, dv(version_label, cl_clause_no, clheading,  
2 SUBSTRING(cl.text,1,600) AS excerpt, clanchor_link, d.official_link  
3 FROM chem_clause_map m  
4 JOIN clauses cl ON cl.id = m_clause_id  
5 JOIN document Versions dv ON dv.id = cl.document_version_id AND dv.is_current=1  
6 JOIN documents d ON d.id = dv.document_id  
7 WHERE m.chemical_id = :chem_id  
8 AND cl.facet = 'MANAGEMENT'  
9 ORDER BY m.relevance_score DESC, d.effective_date DESC  
10 LIMIT 20;  
11
```

# 5.3 使用要求 (SOP + MSDS 第8部分)

```sql
1 --SOP   
2 SELECT title,version_label,body FROM sop_library   
3 WHERE (chemical_id  $\equiv$  :chem_id OR chemical_id IS NULL)   
4 AND scope IN ('HANDLING','STORAGE','PPE');   
5   
6 --MSDS 第8部分（接触控制/个体防护）   
7 SELECT content FROM msds_sections s   
8 JOIN msds Headers h ON h.id  $=$  s.msds_id   
9 WHERE h.chemical_id  $\equiv$  :chem_id AND s.section_no  $= 8;$    
10
```

# 5.4 识别与许可（运输/目录/类别）

```sql
SELECT \* FROM transport_classifications WHERE chemical_id  $=$  :chem_id; -- UN/类别/包装等 2   
3 SELECT catalog_key, is_listed, reference 4 FROM hazard_catalog_flags WHERE chemical_id  $=$  :chem_id; 5
```

# 5.5应急措施(MSDS第4、5、6部分+应急卡)

```sql
1 SELECT section_no, content  
2 FROM msdsSections s JOIN msds Headers h ON h.id=s.msds_id  
3 WHERE h.chemical_id=:chem_id AND section_no IN (4,5,6);  
4  
5 SELECT section, body FROM emergency Cards  
6 WHERE chemical_id=:chem_id OR chemical_id IS NULL;  
7
```

# 6. 数据录入与治理流程（对 MSDS 人工录入友好）

1. 化学品建档：先填 chemicals 与主要别名 names_synonyms；  
2. MSDS录入：

o 建 msdsheaders (提供者、编制依据等)；  
- 逐段录入 msds_sections (1-16)，将结构化数值（如闪点/沸点）同步入 msds_physchem；  
o 将 GHS/H/P、象形图等提炼入 ghs classifications；运输与名录入相应表；

3. 法规条款：

- 录入 documents (三类用 doc_type 区分)  $\rightarrow$  document Versions (设 is_current=1)  $\rightarrow$  clauses (逐条、标注 facet);  
。人工建立 chem_clause_map 连接到具体条款与“管理/许可/储存”等标签；

4. SOP/应急卡：

建立通用SOP模板，再按重点化学品（如甲醛、乙醇、氯气等）创建专属SOP；  
○ 应急卡按三分区（急救/消防/泄漏）制式化；

5. 审校与版本：

document Versions 控制法规换版；msds Headers compiled at 记录 MSDS 时间，替换时保留历史；  
。通过 is_current 与软删除字段（如 is.active）进行历史保留与前端只读当前版；

6. 权限与审计：

。为每张表增加 created_by/updated_by 、操作日志表 (可选), 满足合规可追溯。

# 7. 扩展：跨地域/多法规口径与冲突解决

- 利用 doc_jurisdictions 精确到省市，将同一化学品映射多条不同地域条款；  
前端按用户站点（省/市）优先级排序显示；

- chem_clause_map.relevance_score 结合地域优先规则（本省>国家>其他）与生效时间权重；  
- 冲突提示：当同一主题（如仓储上限）在不同法规存在差异，后端标注 conflict-note （可加到 chem_clause_map.tag 或另建表）。

# 8. 性能与搜索建议

- 全文检索：

o names_synonyms、clauses、msdsSections 建FULLTEXT;  
o 将常用 facet 的条款缓存（Redis）以加速“甲醛  $\rightarrow$  四块结果”装配；

- 过滤条件：  
○地域（province/city）、文档类型（LAW/REGULATION/STANDARD）、状态（在效/已废止）；  
- 结果去重：  
。针对同一条款被多次映射， DISTINCT clause_id 即可。

# 9. 数据示例（以乙醇为例，便于先通路）

乙醇 MSDS 的关键信息在你附件中可直接映射：如“GHS：易燃液体 类别2；信号词：危险；H225；P210...；理化特性（闪点  $13^{\circ} \mathrm{C}$  、初沸点  $79^{\circ} \mathrm{C}$  等）；运输信息（UN1170、类别3、包装II）”。

```sql
1 INSERT INTO chemicals (cas_no, cn_name, en_name, formula, ec_no)  
2 VALUES ('64-17-5', '乙醇[无水}', 'ethanol', 'C2H60', '200-578-6');  
3  
4 -- 同义词  
5 INSERT INTO names_synonyms (chemical_id, name, lang, is_primary)  
6 VALUES (1, '无水酒精', 'zh', 0), (1, 'ethyl alcohol', 'en', 0), (1, 'alcohol anhydrous', 'en', 0);  
7  
8 -- GHS  
9 INSERT INTO ghs_classifications (chemical_id, hazard_class, category, signal_word, pictograms, h_codes, p_codes, source)  
10 VALUES (1, '易燃液体', '2', '危险', ['GHS02'], ['H225'],  
11 ['P210", "P233", "P240", "P241", "P242", "P243", "P280", "P370+P378", "P303+P361+P353", "P403+P235", "P501"', 'MSDS');  
12  
13 -- 运输  
14 INSERT INTO transport_classifications (chemical_id, un_number, primary_class, packing_group, marine POLLutant, remarks)  
15 VALUES (1, '1170', '3', 'II', 0, '运输时防高温、防静电，配备阻火装置等');
```

后端聚合API可直接按以下结构返回：  
```sql
16   
17 -- MSDS索引   
18 INSERT INTO msds Headers (chemical_id,provider，standard BASIS)   
19 VALUES(1,'常州合规思远产品安全技术服务有限公司'，'GB/T16483，GB/T17519')；   
20   
21 -- MSDS第4部分示例   
22 INSERT INTO msdsSections(msds_id，section_no，section_title，content)   
23 VALUES(1,4,'急救措施'，'眼：大量清水冲洗；皮肤：脱去污染衣物并清洗；吸入：新鲜空气休息；食入：漱口就医.....');   
24
```

# 10. 前端/服务输出拼装（四块卡片）

```txt
1 {   
2 "chemical": {"name":"甲醛","cas":"50-00-0"},   
3 "management": [ {"docType":"LAW", "title":"xx法", "clauseNo":"第 $\times$ 条", "excerpt":"......", "link":"..."}],   
4 "use所需要的": {   
5 "sop": [ {"title":"甲醛操作规程-储存","body":"..."} ],   
6 "ppe": "来自MSDS第8部分摘录...."   
7 },   
8 "identification permit": {   
9 "transport": {"UN":"2209","class":"8","packing":"II"},   
10 "catalogFlags": ["key":"危化品目录2015","listed":1,"ref":"安监总局2015年第5号"]   
11 },   
12 "emergency": {   
13 "first aider":"MSDS第4部分.....",   
14 "firefighting":"MSDS第5部分.....",   
15 "spill":"MSDS第6部分....."   
16 }   
17 }   
18
```

# 11. 安全/合规与版权

- 每条 documents/clauses 存官方链接与来源标注；对付费或版权内容保留指向+摘要，不存全文或仅存必要片段；  
- hazard_catalog_flags.reference 填写公告文号，便于追溯；  
- 操作留痕：为关键表加 created_by/updated_by 与审计日志（可另建 audit_logs）。

# 12. 迭代路线图（落地建议）

1. 第 1 周：化学品主数据 + MSDS 结构上线；优先录入高频化学品 (前 100 个)  
2. 第 2-3 周：搭建法规条款库（国家层面）与 chem_clause_map 的人工映射流程/工具  
3. 第4周：完善SOP/应急卡模板，接入地域维度（省级）；实现“自然语言→四卡”API  
4. 持续：全文检索权重调优、缓存、告警（法规换版时提示重审映射）

如果你愿意，我可以把上面的DDL打包成一份.sql 初始化脚本，并附带3-5条“甲醛/乙醇”的样例数据，方便你一键建库与联调前端。
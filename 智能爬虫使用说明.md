# 🤖 智能MSDS爬虫 v2.0 - 使用说明

> **一键爬取，自动识别！** 只需提供链接，自动完成一切！

---

## ✨ 新功能亮点

### 🎯 智能识别
- ✅ **自动提取化学品名称** - 无需手动输入
- ✅ **自动提取mid参数** - 不用手动点击查找
- ✅ **自动创建文件夹** - 以化学品名称命名
- ✅ **一键开始爬取** - 全自动流程

### ⚡ 使用对比

| 功能 | 旧版scrape_msds.py | 新版auto_scrape_msds.py |
|------|-------------------|------------------------|
| 提取mid | ❌ 手动 | ✅ 自动 |
| 提取名称 | ❌ 手动 | ✅ 自动 |
| 创建文件夹 | ❌ 手动命名 | ✅ 自动命名 |
| 操作步骤 | 5步 | 1步 |

---

## 🚀 三种使用方式

### 方式1：图形界面（最简单）⭐

**双击运行**: `智能爬虫.bat`

```
╔══════════════════════════════════════════════════════════╗
║          智能MSDS爬虫 v2.0 - 一键爬取                    ║
║            只需提供链接，自动完成一切                    ║
╚══════════════════════════════════════════════════════════╝

请粘贴化学品MSDS页面的完整链接: [在这里粘贴]
```

**操作步骤**：
1. 双击运行 `智能爬虫.bat`
2. 粘贴链接（如：`http://...?decrypt=xxx`）
3. 回车
4. 等待完成！

---

### 方式2：命令行（推荐）⚡

```bash
# 基础用法
python auto_scrape_msds.py "链接"

# 设置延迟
python auto_scrape_msds.py "链接" --delay 1.0

# 自定义前缀
python auto_scrape_msds.py "链接" --prefix my_msds

# 完整示例
python auto_scrape_msds.py "http://www.hgmsds.com/weixin/msds-list-details?decrypt=xxx" -d 1.0
```

**注意**：链接必须用**引号**括起来！

---

### 方式3：交互模式 💬

```bash
# 不提供链接，进入交互模式
python auto_scrape_msds.py

# 然后按提示输入链接
```

---

## 📋 完整示例

### 示例1：爬取甲苯

**你的链接**：
```
http://www.hgmsds.com/weixin/msds-list-details?decrypt=OEiAuBxcCd5FIb81hGnVYQ%3D%3D
```

**运行命令**：
```bash
python auto_scrape_msds.py "http://www.hgmsds.com/weixin/msds-list-details?decrypt=OEiAuBxcCd5FIb81hGnVYQ%3D%3D"
```

**自动完成**：
```
✓ 化学品名称: 甲苯
✓ CAS号: 108-88-3
✓ mid参数: 8nfuae
✓ 输出目录: msds_甲苯/
✓ 正在获取: 01_化学品及企业标识... ✓
✓ 正在获取: 02_危险性概述... ✓
...
✓ 完成! 成功获取 16/16 个部分
```

---

### 示例2：爬取乙醇

**你的链接**：
```
http://www.hgmsds.com/weixin/msds-list-details?decrypt=3MIzS2IzVB%2BvKQnkqocW%2Fg%3D%3D
```

**运行命令**：
```bash
python auto_scrape_msds.py "http://www.hgmsds.com/weixin/msds-list-details?decrypt=3MIzS2IzVB%2BvKQnkqocW%2Fg%3D%3D"
```

**自动完成**：
```
✓ 化学品名称: 乙醇[无水]
✓ CAS号: 64-17-5
✓ mid参数: dmacpg
✓ 输出目录: msds_乙醇无水/
✓ 开始爬取...
```

---

## 🎯 工作原理

```
┌─────────────────────────────────────────┐
│  你提供带decrypt的链接                   │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  脚本访问链接，解析HTML                  │
│  • 提取化学品中文名                      │
│  • 提取CAS号                             │
│  • 提取mid参数（从hidden input）         │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  自动创建文件夹                          │
│  • 格式：msds_化学品名称                 │
│  • 清理非法字符                          │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  调用scrape_all_parts开始爬取            │
│  • 爬取16个部分                          │
│  • 保存为HTML文件                        │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  完成！打开文件夹查看结果                │
└─────────────────────────────────────────┘
```

---

## 🔑 关键优势

### 1. 真正的一键操作
**旧版**：
```
1. 打开链接
2. 点击任意部分
3. 复制URL
4. 提取mid
5. 运行爬虫
```

**新版**：
```
1. 粘贴链接，完成！
```

---

### 2. 自动命名
**旧版**：
```bash
python scrape_msds.py --mid 8nfuae --output msds_甲苯
                                           ^^^^^^^^ 手动输入
```

**新版**：
```bash
python auto_scrape_msds.py "链接"
# 自动创建 msds_甲苯/ 文件夹
```

---

### 3. 容错性强
- ✅ 自动清理文件名中的非法字符
- ✅ 自动处理URL编码
- ✅ 详细的错误提示
- ✅ 网络异常自动重试

---

## 📝 参数详解

```bash
python auto_scrape_msds.py [链接] [选项]
```

### 必需参数

| 参数 | 说明 | 示例 |
|------|------|------|
| `链接` | 带decrypt的完整URL | `"http://...?decrypt=xxx"` |

**注意**：
- 必须是**目录页**链接（带decrypt参数）
- 不是详情页链接（带mid参数）
- 必须用引号括起来

---

### 可选参数

| 参数 | 简写 | 默认值 | 说明 |
|------|------|--------|------|
| `--delay` | `-d` | `0.5` | 请求延迟（秒） |
| `--prefix` | `-p` | `msds` | 文件夹前缀 |

**示例**：
```bash
# 延迟1秒
python auto_scrape_msds.py "链接" --delay 1.0

# 前缀改为data
python auto_scrape_msds.py "链接" --prefix data
# 输出：data_甲苯/

# 组合使用
python auto_scrape_msds.py "链接" -d 1.5 -p my_msds
# 输出：my_msds_甲苯/
```

---

## 🆚 新旧版本对比

### 场景：爬取甲苯MSDS

#### 旧版（scrape_msds.py）

```bash
# 步骤1：在浏览器中打开链接
# 步骤2：点击任意MSDS部分
# 步骤3：从新URL中复制mid (8nfuae)
# 步骤4：手动输入命令
python scrape_msds.py --mid 8nfuae --output msds_甲苯

# 总耗时：约2-3分钟（包含手动操作）
```

#### 新版（auto_scrape_msds.py）

```bash
# 一步完成
python auto_scrape_msds.py "http://www.hgmsds.com/weixin/msds-list-details?decrypt=OEiAuBxcCd5FIb81hGnVYQ%3D%3D"

# 总耗时：约10-15秒（全自动）
```

---

## ✅ 使用检查清单

在使用前，请确认：

- [ ] 已安装Python 3.6+
- [ ] 已安装依赖库：`pip install requests beautifulsoup4`
- [ ] `scrape_msds.py` 和 `auto_scrape_msds.py` 在同一目录
- [ ] 网络连接正常
- [ ] 链接是**目录页**（带decrypt），不是详情页（带mid）

---

## ❓ 常见问题

### Q1: 提示"找不到scrape_msds.py"

**A**: 确保两个文件在同一目录：
```
你的文件夹/
├── scrape_msds.py          ← 必需
├── auto_scrape_msds.py     ← 必需
└── 智能爬虫.bat             ← 可选
```

---

### Q2: 提示"无法提取化学品名称"

**A**: 检查链接是否正确：
- ✅ 正确：`http://...?decrypt=xxx`（目录页）
- ❌ 错误：`http://...?mid=xxx`（详情页）

---

### Q3: 链接很长，怎么粘贴？

**A**: 三种方法：

**方法1**：使用批处理文件
```bash
双击 智能爬虫.bat，然后粘贴
```

**方法2**：使用交互模式
```bash
python auto_scrape_msds.py
# 然后粘贴链接
```

**方法3**：直接命令行
```bash
python auto_scrape_msds.py "链接"
# 注意：必须用引号
```

---

### Q4: 想批量爬取多个化学品

**A**: 创建批处理脚本 `batch_auto.bat`：

```batch
@echo off
echo 开始批量爬取...

python auto_scrape_msds.py "链接1" --delay 1.0
python auto_scrape_msds.py "链接2" --delay 1.0
python auto_scrape_msds.py "链接3" --delay 1.0

echo 全部完成！
pause
```

---

### Q5: 输出文件夹名称不满意

**A**: 使用 `--prefix` 参数：

```bash
# 默认：msds_甲苯/
python auto_scrape_msds.py "链接"

# 自定义：data_甲苯/
python auto_scrape_msds.py "链接" --prefix data

# 自定义：化学品_甲苯/
python auto_scrape_msds.py "链接" --prefix 化学品
```

---

## 🎓 高级用法

### 1. 集成到Python脚本

```python
from auto_scrape_msds import auto_scrape

# 直接使用
success = auto_scrape(
    decrypt_url="http://...?decrypt=xxx",
    output_prefix="my_data",
    delay=1.0
)

if success:
    print("爬取成功！")
```

---

### 2. 批量处理URL列表

```python
urls = [
    "http://...?decrypt=xxx1",  # 甲苯
    "http://...?decrypt=xxx2",  # 乙醇
    "http://...?decrypt=xxx3",  # 丙酮
]

for url in urls:
    auto_scrape(url, delay=1.5)
    time.sleep(2)  # 化学品之间等待2秒
```

---

### 3. 从文件读取URL列表

创建 `urls.txt`：
```
http://www.hgmsds.com/weixin/msds-list-details?decrypt=xxx1
http://www.hgmsds.com/weixin/msds-list-details?decrypt=xxx2
http://www.hgmsds.com/weixin/msds-list-details?decrypt=xxx3
```

运行脚本 `batch_from_file.py`：
```python
from auto_scrape_msds import auto_scrape
import time

with open('urls.txt', 'r', encoding='utf-8') as f:
    urls = [line.strip() for line in f if line.strip()]

for i, url in enumerate(urls, 1):
    print(f"\n[{i}/{len(urls)}] 正在处理...")
    auto_scrape(url, delay=1.0)
    time.sleep(2)

print("\n全部完成！")
```

---

## 📚 文件说明

| 文件 | 说明 | 是否必需 |
|------|------|---------|
| `auto_scrape_msds.py` | 智能爬虫主程序 | ✅ 必需 |
| `scrape_msds.py` | 基础爬虫库 | ✅ 必需 |
| `智能爬虫.bat` | Windows图形界面 | ⭐ 推荐 |
| `智能爬虫使用说明.md` | 本文件 | 📖 参考 |

---

## 🎉 开始使用

### 最简单的方式

1. **双击运行**：`智能爬虫.bat`
2. **粘贴链接**：复制化学品页面URL
3. **回车等待**：自动完成
4. **打开文件夹**：查看结果

就这么简单！🚀

---

## 💡 小技巧

### 技巧1：快速获取链接

1. 打开 http://www.hgmsds.com
2. 搜索化学品
3. 点击化学品名称
4. 复制浏览器地址栏的URL
5. 粘贴到智能爬虫

---

### 技巧2：一次爬取多个

复制多个链接，一次性粘贴到批处理脚本中：

```batch
python auto_scrape_msds.py "链接1"
python auto_scrape_msds.py "链接2"
python auto_scrape_msds.py "链接3"
```

---

### 技巧3：自定义文件夹结构

```bash
# 按日期分类
python auto_scrape_msds.py "链接" --prefix msds_20251010

# 按项目分类
python auto_scrape_msds.py "链接" --prefix project_a
```

---

## ⚠️ 注意事项

1. **合法使用** - 仅供学习研究
2. **请求延迟** - 建议≥0.5秒
3. **网络稳定** - 需要稳定的网络连接
4. **链接正确** - 必须是目录页链接（带decrypt）

---

**版本**: v2.0  
**更新日期**: 2025-10-10  
**Python要求**: 3.6+

---

**祝使用愉快！** 🎊

